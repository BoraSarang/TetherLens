# TetherLens — 프로젝트 특화 에이전트 가이드

> **적용 범위**: TetherLens 프로젝트 전용
> **읽기 순서**: 공통(`~/.config/opencode/AGENTS.md`) → 특화(이 파일) → AGENTS.macos.md → docs/세부
> **버전**: v1.6.0-local (2026-07-28) — v1.6 정렬

## 1. 언어 규칙 (필수)

- **모든 문서는 한국어로 작성**: PRD.md, PLAN.md, DESIGN.md, CHANGELOG.md 등
- **코드**: 주석/변수명/로그는 영어 허용 (Swift/Apple 표준 준수)
- **사용자 응답**: 한국어 (전문 용어/파일명/코드는 영어 원형 유지)

## 2. 프로젝트 진입 시 필수 탐색 순서

1. `docs/` 폴더 전체 스캔: `PRD.md`, `PLAN.md`, `DESIGN.md`, `tests/`
2. 환경 파일 확인: `Package.swift`, `scripts/package.sh`, `README.md`
3. 컨벤션 확인: `.editorconfig`, `.swiftlint.yml` (있으면 최우선 준수)
4. `bd list`로 열린 버그 확인
5. 이 파일(`AGENTS.local.md`) 존재 확인 후 읽기

## 3. 프로젝트 설정 (v1.6)

```bash
APP_NAME="TetherLens"
BUNDLE_ID="com.tetherlens.app"
CODESIGN_IDENTITY="Apple Development: leeborasarang@gmail.com (HLQNBZHQQN)"
```

## 4. 문서 구조

| 파일 | 위치 | 상태 | 설명 |
|------|------|------|------|
| `PRD.md` | `docs/` | ✅ | 제품 요구사항 |
| `PLAN.md` | `docs/` (symlink) | ✅ | `docs/plans/PLAN_v0.1.0.md` 연결 |
| `DESIGN.md` | `docs/` | ❌ | 필요 시 생성 |
| `CHANGELOG.md` | `docs/` | ✅ | 변경 이력 |
| `TODO.md` | `docs/` | ✅ | 작업 추적 (bd 우선, 보조 참조) |
| `CLAUDE.md` | `docs/` | ❌ | 존재하지 않음 (필요 시 생성) |
| `tests/v0.1.0.md` | `docs/tests/` | ✅ | Phase 0 PoC 테스트 계획 |

## 5. Beads (bd) 이슈 트래커

이 프로젝트는 **bd (beads)** 로 모든 태스크를 추적합니다.

### 빠른 명령어

```bash
bd ready              # 작업 가능 항목 찾기
bd show <id>          # 이슈 상세 보기
bd update <id> --claim  # 작업 할당
bd close <id>         # 작업 완료
bd prime              # Beads 컨텍스트 리프레시
```

### 규칙

- `bd`로만 태스크 추적 — `todowrite`, `TaskCreate`, markdown TODO 목록 금지
- `bd prime` 실행하여 워크플로 컨텍스트 확인
- `bd remember`로 지속적 지식 저장 — MEMORY.md 파일 금지

## 6. Non-Interactive Shell Commands

셸 명령어에 `-i` 인터랙티브 프롬프트 방지를 위해 항상 `-f` 사용:

```bash
cp -f source dest       # NOT: cp source dest
mv -f source dest       # NOT: mv source dest
rm -f file              # NOT: rm file
rm -rf directory        # NOT: rm -r directory
```

## 7. 구현 규칙

### 7.1 새 기능/수정 전 체크리스트
1. `bd ready` 또는 `bd list`로 작업 확인
2. `docs/` 문서에서 관련 요구사항/태스크 확인
3. 기존 유사 구현 `grep`으로 검색
4. 영향 범위 확인 (변경할 파일 import/사용처 추적)
5. **구현 전 사용자에게 계획 확인 요청**

### 7.2 v1.6 Definition of Done (DoD)
1. `./build_and_run.sh debug macos` 성공
2. DebugPanel에서 MACOS 로그 확인 (ERROR 0개)
3. 필요한 경우 전체 복사 로그를 bd notes에 첨부
4. CHANGELOG.md에 `[macOS]` 태그로 기록
5. PLAN.md 태스크 상태 업데이트 (✅/⬜)
6. 변경 요약: 무엇 / 왜 / 어떻게 (한국어 불릿)
7. `bd close --reason "platform:macos / 수정:..."`

### 7.3 금지 사항
- ❌ `todowrite`로 태스크 관리 (대신 `bd` 사용)
- ❌ 영어로 문서 작성 (기존 문서도 한국어 유지보수)
- ❌ PRD/PLAN 문서 없이 구현 시작
- ❌ `docs/` 문서를 읽지 않고 코드 수정

## 8. macOS/Swift 프로젝트 규칙 (v1.6)

- `Package.swift` (SPM) 기반 — Xcode 프로젝트 직접 수정 금지
- macOS 14.0+ 타겟, Swift 6, AppKit + SwiftUI 혼용
- Info.plist는 `Resources/Info.plist`에서 관리
- 빌드/실행: `./build_and_run.sh debug macos` (v1.6 디스패처)
- 실제 빌드 로직: `scripts/build-macos.sh`
- 테스트: `./scripts/test.sh` — `Tests/TetherLensTests/` (Swift Testing, 32개/7스위트, v0.22 도입)

## 9. Git 브랜치 네이밍 (v1.6)

```bash
feat/{platform}-{feature}  # 예: feat/macos-debug-panel
fix/bd-{id}-{platform}     # 예: fix/bd-123-macos
chore/{platform}-{task}    # 예: chore/macos-deps
```

## 10. 문서 동기화

- 코드 수정과 문서 업데이트는 **같은 작업 단위에서 처리**
- 문서 먼저, 코드 나중 (또는 동시에)
- PLAN.md T-번호를 커밋 메시지에 참조: `feat(profile): T-108 SSID 자동 등록`

## 11. 커뮤니케이션 규칙

| 상황 | 대응 |
|------|------|
| 요청 이해 안 됨 | 2~3개 해석안 제시 후 선택 요청 |
| 에러 발생 | 에러 원문 + 원인 + 해결책 3단계 |
| 변경 완료 | 무엇/왜/어떻게 불릿 요약 (한국어) |
| 계획 질문 | PLAN 기준 현황 + 선택지 제시 |
| 모호한 요청 | 문서(PRD/PLAN) 근거로 질문 |

## 12. Agent Context Profiles

- **Conservative (기본)**: `bd`로 태스크 추적. git 커밋/푸시는 명시적 요청 시에만 수행. 핸드오프 시 변경 파일, 검증 결과, 제안 명령어 보고.
- **Minimal**: `bd prime` 지침 사용. git 정책은 Conservative과 동일.
- **Team-maintainer**: 저장소가 명시적으로 opt-in 한 경우에만 사용. 세션 종료 시 beads 종료, 품질 게이트, 커밋/푸시 가능.

## 13. Session Completion 프로토콜 (v1.6 DoD)

1. 남은 작업에 대해 beads 생성
2. 품질 게이트 실행 (코드 변경 시): 빌드, 린트
3. 이슈 상태 업데이트 — 완료된 작업 종료, 진행 중 작업 업데이트
4. 핸드오프 — 변경 요약, 검증 결과, 이슈 상태, 막힌 sync/커밋/푸시 단계 보고

**핵심 규칙:**
- 명시적 사용자 지시가 이 프로토콜보다 우선
- 명시적 권한 없이 커밋/푸시 금지
- sync/푸시가 막히면 정확한 명령어와 에러를 보고하고 중단

<!-- BEGIN BEADS INTEGRATION v:1 profile:minimal hash:6cd5cc61 -->
<!-- END BEADS INTEGRATION -->
<!-- BEGIN BEADS CODEX SETUP: generated by bd setup codex -->
<!-- END BEADS CODEX SETUP -->
