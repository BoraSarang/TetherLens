# TetherLens 테스트 가이드 — v0.22.x (자동화 도입)

> 사용법: `./scripts/test.sh`
> 1) 자동화 단위 테스트(swift test, 32개) 실행
> 2) 통과 시 자동화 불가 수동 체크리스트 10항목 출력
> 3) 실패 시 실패 목록 + 로그(`/tmp/tetherlens-test-*.log`) 안내, exit 1

## 자동화 테스트 대상 (Swift Testing)

| 스위트 | 파일 | 검증 내용 |
|--------|------|-----------|
| DataStoreTests | `DataStoreTests.swift` | v1~v8 마이그레이션, 테이블/인덱스/FK, session_id 컬럼 |
| ProfileManagerTests | `ProfileManagerTests.swift` | 프로필 CRUD, autoRegister, 누적 델타, 일/월 요약, 세션, IP dedup/merge/범위, cleanupOldLogs, CSV 이스케이프, appTraffic 집계 |
| SettingsManagerTests | `SettingsAndSavingTests.swift` | 기본값, 저장/조회, 잘못된 모드 폴백, 리셋, MenuBarMode |
| SavingModeManagerTests | `SettingsAndSavingTests.swift` | 임계값 단일화, shouldAutoActivate |
| SystemProcessesTests | `SystemAndLocalizedTests.swift` | 시스템/사용자 프로세스 분류 |
| LocalizedTests | `SystemAndLocalizedTests.swift` | ko/en 반환 |
| HotspotDetectorTests | `SystemAndLocalizedTests.swift` | 안드로이드 SSID/게이트웨이 대역 판정 (v0.22.1) |

## 자동화 불가 (수동) — test.sh이 안내

메뉴바/팝오버 UI, 온보딩·권한, 실제 핫스팟 감지, 절약모드 hosts 차단(sudo),
nettop/ping 실측, 알림, DebugPanel, 저전력 모드 — 상세 절차는 `v0.21.0_macos.md` T-01~T-56.

## v0.22.1 수동 테스트 실행 결과 (2026-08-06)

| # | 항목 | 결과 |
|---|------|------|
| 1 | 메뉴바 아이콘 + 오른쪽 클릭 더보기 | ✅ |
| 2 | 팝오버 (속도/잔여/QoS) | ✅ |
| 3 | 온보딩 창 + 위치 권한 | ✅ |
| 4 | SSID/연결 타입 표시 | ✅ (Android 핫스팟 → "안드로이드 핫스팟" 표시, v0.22.1 수정 검증) |
| 5 | hosts 차단 (절약모드 sudo) | ✅ (`/etc/hosts`에 `# TetherLens SavingMode` 마커 + 4줄 추가 확인) |
| 6 | 실시간 속도 갱신 | ✅ |
| 7 | QoS 게이지/핑 | ✅ |
| 8 | 할당량 경고 알림 (50/80/95/100%) + 핑 경고 | ✅ |
| 9 | DebugPanel (Cmd+D) ERROR 0건 | ✅ |
| 10 | 배터리: 저전력 모드에서 위치/IP 갱신 중지 | ✅ |

## 실행 예시

```bash
./scripts/test.sh
# [PASS] 자동화 테스트 전체 통과 (32개)
# → 수동 테스트 체크리스트 10항목 출력 후 안내
```

테스트 추가 시: `Tests/TetherLensTests/`에 파일 추가 → `swift test` → `./scripts/test.sh` 검증.
